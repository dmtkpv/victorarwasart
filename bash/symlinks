cd "$(dirname "$0")"

DIST="$PWD/../.git/hooks"
HOOKS=("pre-push" "post-receive" "push-to-checkout")

for hook in "${HOOKS[@]}"
do
     chmod +x "$PWD/$hook"
     ln -sf "$PWD/$hook" "$DIST/$hook"
done
